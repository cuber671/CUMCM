# 七 实验与结果分析
- 数据审计表：附件2的三分区样本量、有效 WordPiece 数、audio/vision内容零行率和附件3联合缺失统计先行报告。
- 缺失率×缺失位置×缺失模态三维实验覆盖 `{text,audio,vision}×{头,中,尾}`；所有架构共享固定 mask library，模型仅改变训练 seed并报告 mean±std。
- 鲁棒性同时报告 utility、绝对下降和相对下降；MAE utility 使用 `1-MAE/6`，Pearson utility 使用 `(r+1)/2`。
- 主消融只保留 Missing Augmentation、Missing State、Masked Attention、Reliability Gate；GRU/Transformer、focal/MSE、Feature-dependent proxy、课程和潜空间正则列入补充实验。
- P1 验证四指标表（schema/文本 cos≈1/行为一致性/边界类型回放）先入此章；附件3只作行为分析和最终推理，不作准确率验证。
