# 四 问题一：多模态特征提取与时序对齐
- 素材最全章：docs/问题一建模方案.md v2.2 全部实测数字可直接引用。附件2有效语义位置的 audio 非零率为 train/valid/test = 99.915%/99.732%/100.000%，vision 非零率为94.473%/94.293%/93.925%；bitwise 复制、标点继承和截断旁路映射按冻结 schema 描述。
- 附件1全量机器验收（2026-09-24）：100/100 样本覆盖，p1.v2 schema 0 违例，7 条 test 重叠样本 `text_bert` 逐位相等且 `text` 最小 cosine=1.000000；自生成 audio/vision 非零率为 1.00000/0.94704，均在附件2基准 ±2pp 内；alignment status 为 ok/review/rollback=43/33/24。
- 结构：数据事实 → 统一坐标（2.1/2.2/2.3）→ 特征生成 → 边界规则 → 四指标验证 → 结果表。
- 注意：text=BERT 逐 token 输出（非复制）务必与数据描述自洽；维度双空间声明入正文；机器指标已完成，典型样本人工回放图和审计表仍需补入正文。
