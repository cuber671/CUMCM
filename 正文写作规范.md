# 正文写作规范

## 1. 写作目标

本文正文的目标不是记录全部实验过程，而是让评委沿着一条清楚的论证链快速判断：

1. 每一问要解决什么问题；
2. 为什么需要所采用的模型；
3. 模型如何由输入得到输出；
4. 哪些实验或指标支持结论；
5. 结论适用到什么范围，仍有哪些限制。

正文应从“完整实验记录”改为“面向评委的论证文本”。缺少的表格、文件和代码属于交付补充；行文修改首先解决逻辑顺序、术语负担和证据呈现问题。

### 1.1 可读性优先

论文的学术性来自问题定义、模型推导、实验设计和证据链，不来自华丽词汇或复杂句式。正文以“准确、直接、可读”为首要标准：

- 能用普通词准确说明时，不使用更复杂的替代表达；
- 能用一个短句说明时，不用多层定语、破折号和括号包装；
- 专业性通过变量、公式、实验和数字体现，不通过抽象名词堆叠体现；
- 句子复杂度只在表达必要的条件、因果或限定关系时增加；
- 修改后应让第一次接触本模型的评委能够顺畅读懂，而不是只让熟悉作者术语的人能够理解。

推荐的判断标准是：删去一个形容词或包装性短语后，技术含义是否损失。如果没有损失，就删除。正文不追求“像论文”，而追求让读者看懂论文。

## 2. 全文主线

全文及每个核心小节统一采用：

> 任务 → 难点 → 方法 → 证据 → 结论 → 边界

每个问题都应回答以下六个问题：

- 任务：题目要求输出什么；
- 难点：数据或模型的主要困难是什么；
- 方法：本文采取哪几步处理；
- 证据：用什么数据、指标或对照验证；
- 结论：实验支持什么判断；
- 边界：该判断不应被扩大到什么范围。

不要先罗列模型名称，再让读者自行猜测这些模型为什么出现。

### 2.1 漏斗式开篇和写作前规划

问题背景和摘要采用由宽到窄的漏斗结构：先说明多模态情感预测的实际背景，再收窄到本题的三项任务，最后落到本文的模型和结果。结论部分按同一顺序逐点回应，不引入摘要和正文没有论证的新观点。

动笔前先制作一页“章节故事板”，为每节写下三项内容：本节唯一主张、支撑该主张的图表或公式、下一节将使用的输出。若一节无法写出唯一主张，应拆分或合并；若主张找不到证据，应删除、补实验或降低结论强度。

每章开头可用一小段说明“首先……然后……最后……”，但只交代路线，不重复小节标题，也不堆叠实现参数。

## 3. 章节开头模板

问题一、问题二、问题三的章节开头统一使用“输入—处理—输出”三行结构：

> **输入：** ……
>
> **处理：** ……
>
> **输出：** ……

示例：

> **输入：** 附件1的100条原始视频及文本标注。
>
> **处理：** 建立文本位置、音视频时间区间和50位统一网格之间的映射。
>
> **输出：** 统一时序特征、观测状态和位置—物理时间映射。

章节结尾应明确本章输出如何成为下一章输入，例如：

> 问题一输出的统一位置、观测状态和位置—时间映射，作为问题二训练和问题三解释的共同输入定义。

### 3.1 三问章节的输入—处理—输出写法

三问章节的输入—处理—输出行必须明确给出三件事：上一章交付什么、本章用什么、本章向下一章交付什么。禁止只写“本章将……”，因为它不交代输入和输出的接口关系。

具体而言：

- **输入行**应列出数据来源（含维度与口径）、上一章交付的状态量或模型产物，以及本节所用的可选参照。例如：

  > **输入：** 附件2 对齐特征（语音 74 维、视觉 35 维、文本 768 维）与问题一交付的可用状态标注。

- **处理行**应概述本节的核心动作（构建、训练、归因、验证），不必展开公式。动词应使用 §5.4 给出的“构建/训练/计算/比较/检验”等具体词。例如：

  > **处理：** 构建 MRFN（缺失状态编码、可用性约束注意力、可靠性门控三层），在 31 个固定场景上训练、评测并筛选改进变体。

- **输出行**应列出本章向下一章（或全文）交付的具体对象，包括模型、关键数字、可重用的映射。例如：

  > **输出：** 冻结预测器 MRFN 与补充变体 MRFN+，以及测试集终评、31 场景综合效用、附件3 全量预测。

章节结尾的过渡句也应按相同原则写：

- 不写“接下来讨论……”，因为它只是元叙述；
- 应写“问题二输出的预测器与可解释接口，作为问题三归因框架的输入”。

## 4. 第二章问题分析模板

第二章不直接展开全部公式和机制。问题一、问题二、问题三分别用“难点一句、思路一句、步骤三句、产出一句”说明，单个问题建议控制在八行以内。

问题二的推荐表达：

> 局部缺失会使固定权重融合失效。为使模型区分可用信息与缺失信息，本文依次完成三步：定义每个位置、每个模态的观测状态；限制不可用位置参与跨模态交互；根据模态覆盖率调整融合权重。随后通过模型阶梯、组件消融和31个缺失场景验证各机制的作用。最终输出极性和强度预测，并分析缺失模态、缺失率、缺失位置和缺失形态对性能的影响。

问题分析只交代路线，不在此处堆叠所有实现参数、实验纪律和负结果。详细参数放入相应建模或实验小节。

## 5. 术语命名规则

### 5.1 允许保留的术语

只保留以下三类术语：

1. 题目或文献中的标准术语：CTC、GRU、Shapley值、积分梯度、macro-F1等；
2. 全文反复使用、且确实需要简称的模型名：MRFN、MRFN+；
3. 不能用普通词准确替代的数学对象：观测状态、可用状态、综合效用分等。

### 5.2 新术语的准入条件

只有同时满足以下条件才可以新命名：

- 该对象至少在全文中出现三次；
- 名称比普通描述明显更短；
- 名称描述对象或运算，不只是表达作者评价；
- 首次出现时给出一句白话定义。

如果只是出现一两次，直接用普通描述，不要造名词。

### 5.3 推荐替换

| 当前表达 | 正文推荐表达 |
|---|---|
| 坐标资产 | 统一时序坐标与映射 |
| 公共接口 | 共同使用的坐标定义 |
| 契约 | 数据规范 |
| 选型分 | 验证集综合分 |
| 冻结口径 | 固定评测口径 |
| 受控差异点 | 单因素对照 |
| 八轮候选 | 八轮改进实验 |
| 文本主导脆弱性 | 文本缺失造成的退化更大 |
| 让模型显式“知道”缺失 | 将可用状态作为模型输入 |
| 缺失成为显式输入变量 | 缺失状态进入模型输入 |
| 同构贯通 | 使用相同的缺失构造规则 |

原则是：术语应描述对象、变量或运算，不应包装作者对方法的评价。

### 5.3.1 包装性表达黑名单（严格禁用）

以下表达将方法或模型的“特征”包装成“评价”，违反 §5.2 新术语准入条件。除非在引号内引用他人原话，否则一律删除或改写：

- “闭环”——指代输入输出环式关系时，改写为具体的输入输出接口声明；
- “贯通”——指代跨章节共用时，改写为“使用相同的缺失构造规则”或“复用同一映射”；
- “同构”——指代跨场景规则一致时，改写为“按相同取样规则生成”；
- “统一框架”——指代三层结构时，改写为具体的三层机制名称；
- “充分利用”——指代三模态输入均参与时，改写为“三模态输入均参与融合；报告对应的贡献或消融结果”；
- “鲁棒性增强”——指代训练改进时，改写为“在指定缺失场景下退化率由 A 降至 B”；
- “显著优于”——指代比较结果时，改写为“指标由 A 提高到 B，相对变化 C；若无统计检验则不写‘显著’”；
- “大幅提升”——指代性能变化时，改写为“绝对差值/相对变化/区间”；
- “三问共同底座”——指代跨章共用资源时，改写为“问题一输出的统一坐标与映射，被问题二训练与问题三解释共同使用”；
- “机制优势”——指代组件价值时，改写为“该组件使 31 场景均值 𝒮 下降/上升 X”。

判断标准：删去该词后，如句子仍能说明“做了什么事、效果是多少”，则该词可删除。

### 5.4 正文常用词规范

#### 对象词

同一对象全文只保留一个名称，不交替使用近义词。推荐固定为：

| 含义 | 统一用词 | 避免混用 |
|---|---|---|
| 数据可用情况 | 观测状态、可用状态 | 信息状态、有效状态、可见状态 |
| 被抹除部分 | 缺失位置、缺失区间 | 空白区间、损坏片段、失效区域 |
| 模型输出分类 | 极性预测 | 情感判断、分类判断、类别识别 |
| 模型输出回归 | 强度预测 | 情感分数预测、连续值判断 |
| 模型比较 | 基线、变体、对照模型 | 版本、方案、选手模型 |
| 数值支持 | 证据、结果、实测值 | 证明、印证、佐证一切 |

#### 动词

优先使用能说明具体动作的动词：

- “定义”用于给出变量、状态或指标；
- “构造”用于生成缺失场景、特征或对照输入；
- “计算”用于得到指标或贡献值；
- “比较”用于两个或多个对象的差异；
- “检验”用于有预设判据的验证；
- “观察到”用于实验事实；
- “说明”用于由事实支持的有限解释；
- “表明”只用于证据较充分、方向稳定的结论。

少用“实现了、赋能了、刻画了、揭示了、印证了、支撑了”等泛化动词，除非句子同时说明具体对象和证据。

#### 结论强度

结论措辞应与证据强度匹配：

| 证据情况 | 推荐措辞 | 避免措辞 |
|---|---|---|
| 单次结果或小样本现象 | “观察到”“在本实验中表现为” | “证明”“必然” |
| 多场景、方向一致 | “结果表明”“支持……判断” | “完全说明”“充分证明” |
| 有显著性或预设门槛通过 | “通过检验”“达到预设门槛” | “取得突破”“显著领先” |
| 未做外部验证 | “适用于本文数据和场景” | “具有普适性”“可直接推广” |
| 存在失败项或反例 | “部分成立”“未观察到预期现象” | “总体有效”“基本解决” |

#### 常见替换

| 不推荐 | 推荐 |
|---|---|
| 效果显著 | 指标由A提高到B，相对提高C；若无统计检验，不写“显著” |
| 贡献较大 | 该组件使指标变化C，或在排名/消融中位于…… |
| 鲁棒性明显提升 | 在指定缺失场景下，退化率由A降至B |
| 模型能够有效处理缺失 | 在……场景中，模型的……指标为…… |
| 充分证明 | 支持……判断，或“通过预设检验” |
| 大幅、明显、显著 | 直接给出差值、相对变化或区间 |
| 充分利用多模态信息 | 三模态输入均参与融合；再报告对应贡献或消融结果 |
| 具有较强泛化能力 | 在训练/验证/测试的具体划分上表现为……；不要外推到未验证数据 |
| 优越性、先进性、创新性 | 本文新增的具体机制及其可量化结果 |

#### 英文缩写

标准术语首次出现时写“中文名（英文全称，缩写）”，后文只保留缩写。例如“积分梯度（Integrated Gradients，IG）”。已经约定的 Acc、MAE、Pearson、macro-F1 不重复展开，但表头和正文必须保持同一写法。

### 5.5 叙述主体和模型拟人化

全文叙述主体统一使用“本文”“模型”“实验结果”，不在同一章节交替使用“我们”“本研究”“算法”“网络”指代同一对象。

除非有明确的数学定义，不使用“模型知道、理解、认为、感受到、主动选择”等拟人化表达。推荐改为：

- “模型知道缺失” → “可用状态作为输入变量”；
- “模型理解文本” → “文本编码器产生文本表示”；
- “模型主动降低语音权重” → “缺失率升高时，门控输出中的语音权重下降”。

“学习到”“捕捉到”“利用了”等词只有在有对应变量、消融或归因证据时才使用。

### 5.6 数据和标签用词

正文叙述使用中文，代码字段、表头或正式实验标签才保留英文。推荐统一为：

- `clean` 作为表格或代码标签；正文写“干净输入”或“无缺失输入”；
- `valid` 作为文件标签；正文写“验证集”；
- `test` 作为文件标签；正文写“测试集”；
- `seed` 作为实验字段；正文写“随机种子”；
- `text/audio/vision` 作为代码字段；正文写“文本/语音/视觉”。

同一段落不要在“干净、clean、完整输入”之间无说明地切换。

## 6. 句子、数字和证据格式

### 6.1 句子负担

一段只承担一个主要论点，一句只承担一个主要判断。出现以下任一情况时优先拆句：

- 同时出现两个以上破折号或括号；
- 同时包含方法、数字、解释和限制；
- 主语在句中途改变；
- 句子超过三个分句；
- 删除任意一个插入语后，主句仍无法顺畅阅读。

推荐用“事实句—解释句—限制句”，不要用一个长句包办三层逻辑。

### 6.2 数字和单位

数字首次出现时同时说明对象、数据集划分和口径。全文统一以下格式：

- 百分比使用同一种形式，不混用“0.667”和“66.7%”而不加说明；
- 百分点变化写“提高1.93个百分点”，相对变化写“提高2.9%”，两者不可混称；
- 时长统一用秒，缺失率统一用百分比或0—1比例；
- 区间、阈值和上下界明确写出单位；
- 表格、图注和正文的小数位保持一致；
- 单模型均值、种子均值和多种子集成必须在数字旁标明。

### 6.3 证据来源

正文中的重要判断必须属于以下四类之一，并能回指来源：

1. **定义：**由公式、算法规则或题面约束给出；
2. **实测：**来自表格、图、日志或结果文件；
3. **解释：**基于实测结果提出的有限机制解释；
4. **假设：**实验前登记、尚未验证或仅部分成立的判断。

解释不能写成实测事实，假设不能写成已证结论。没有对应数字、图表或引用时，避免使用“证明、验证、说明了机制”等强结论。

### 6.4 过渡句

每个小节末尾至少完成一项过渡：

- 总结本节得到的结果；
- 指出下一节将使用的输入；
- 说明尚未解决的限制。

不要用“综上所述”“由此可见”连续串联多个没有新信息的总结句。

### 6.5 前后文一致性

正文修改不能只看单个段落，还要检查同一对象在摘要、正文、表格、图注和结论中的说法是否一致。重点检查以下七类一致性：

1. **术语一致：**同一对象只使用一个正式名称。例如统一使用“观测状态”“可用状态”“缺失区间”，不要在不同章节改称“有效状态”“信息状态”或“空白片段”。
2. **模型一致：**明确区分 MRFN、MRFN+、单模型均值和三种子集成。不能在一处称 MRFN+ 为正式模型，另一处又把 MRFN+ 的结果写成 MRFN 的结果。
3. **数据口径一致：**每个数字都核对数据集划分、模型版本、随机种子、评估场景和汇总方式。验证集选择结果、测试集终评结果和附件3/附件4无标签推理结果不得混写。
4. **数字一致：**摘要、正文、表格、图注中的同一结果必须相同；若因四舍五入不同，统一保留位数并说明。Acc、macro-F1、MAE、Pearson的单位和显示格式全文一致。
5. **符号一致：**同一个变量、指标或状态在公式、正文、图表和代码说明中使用同一符号。特别核对 $a$（可用状态）、$o$（自然观测）、$b$（合成缺失）、$g$（门控权重）等符号是否被重复赋予其他含义。
6. **结论范围一致：**摘要和总结不得比正文证据更绝对。正文报告“部分成立”或“未观察到预期现象”时，摘要不能改写成“验证有效”；只在特定场景成立的结论不得扩展成普遍规律。
7. **引用关系一致：**每个图、表、公式和附录编号都应与正文引用对应，避免正文引用不存在的编号，或生成了表格却没有在正文引入。

当前论文尤其需要核对：

- “MRFN正式模型”和“MRFN+附件3/附件4推理模型”的主次关系；
- “主导模态”“IG局部证据”和“全局TOP-k文本证据”三个概念是否混用；
- 摘要中的“全局跨模态证据均为文本”是否明确限定为 IG TOP-k 统计；
- 31个缺失场景、验证集和测试集的数字是否使用同一汇总口径；
- 表格中百分数与0—1比例是否与正文叙述一致；
- 假设的状态（成立、部分成立、未通过、待人工签核）是否在摘要、正文和总结中保持一致。

建议在全文定稿前建立一张“口径对照表”，至少列出：模型名称、数据集划分、核心指标、结果数值、结果来源、正文首次出现位置。任何摘要或结论数字都从该表回填，不手工凭记忆改写。

### 6.6 口径对照表模板（论文专属）

本论文建议在定稿前建立并维护下表，作为全文数字回填的唯一基准。表格可在附录或正文首部出现一次，正文每次出现关键数字时应在该表标注首次出现位置。

| 数字 | 数据集划分 | 模型/变体 | 指标 | 汇总方式 | 来源文件 | 首次出现位置 |
|---|---|---|---|---|---|---|
| 66.71% | 测试集（封版） | MRFN 单模型 | Acc | 单次终评 | test_results.json | §5.5 |
| 67.45% | 测试集 | MRFN+ 单模型 | Acc | 三种子均值 | test_second_results.json | §5.5 |
| 68.64% | 测试集 | MRFN+ 集成 | Acc | 三种子概率 argmax | test_second_results.json | §5.5 |
| 0.170 | 验证集 | MRFN+ | F 项 IG–LOO Spearman | 单点 LOO | runs/p3/fidelity.json | §6.4 |
| 0.716 | 验证集（728 条） | MRFN+ 集成 | $g$–$\varphi$ Spearman（文本） | 单次计算 | runs/p3/gphi.json | §6.4 |
| 90.7% | 验证集（728 条） | MRFN+ 集成 | $g$–$\varphi$ argmax 一致率 | 单次计算 | runs/p3/gphi.json | §7.2 |
| 0.575 | 验证集（728 条） | MRFN+ 集成 | MAE | 三种子回归均值 | error_attribution.json | §7.4 |
| 0.680 | 验证集（728 条） | MRFN+ 集成 | Pearson | 三种子回归均值 | error_attribution.json | §7.4 |
| 1.2142 | 验证集 | — | 温度 $T$ | NLL 拟合 | test_second_results.json | §5.6、§6.5 |

口径标注规则：

- 数据集划分写明“训练 / 验证 / 测试 / 附件 3 / 附件 4”，不写“总体”；
- 模型名区分 MRFN、MRFN+、单模型、三种子集成；
- 指标使用论文锁定的写法（Acc、macro-F1、𝒮、𝒟_𝒮、MAE、Pearson 等）；
- 汇总方式区分单模型均值、种子均值、三种子集成；
- 来源文件给出可定位的具体文件路径（与交付包 manifest 对应）。

## 7. 引号、括号和口号

### 7.1 引号的使用

引号只用于以下情况：

- 题目中的原始术语；
- 数据文件或代码中的字段名；
- 第一次定义的特殊标签；
- 明确引用他人的原话。

以下表达应删除引号并改成可检验的陈述：

- “让模型显式知道”；
- “公共接口”；
- “文本主导”；
- “无干净代价换取鲁棒性”；
- “同构贯通”；
- “坐标资产”。

### 7.2 括号的使用

一个句子最多承载一个必要括号。以下内容不要全部塞进括号：

- 数据集划分；
- 比较对象；
- 数值口径；
- 结论限制；
- 例外情况。

如果括号内超过一行，优先拆成独立句子。

### 7.3 口号改写原则

所有带评价色彩的口号，都改为“指标—比较对象—结论边界”。

不推荐：

> 缺失增强训练以无干净代价换取鲁棒性增益。

推荐：

> 与仅使用干净样本训练相比，缺失增强训练使31个缺失场景的综合效用提高0.030，clean综合效用提高0.020。因此，在本实验设置下没有观察到干净性能下降。

不推荐：

> 让模型显式“知道”每个位置每个模态是否被观测到。

推荐：

> 将每个位置、每个模态的可用状态作为输入变量，使模型区分“特征值为零”和“该位置不可观测”两种情况。

## 8. 段落结构

### 8.1 方法段

方法段按以下顺序写：

1. 先说为什么需要该定义或模块；
2. 再给数学定义或结构；
3. 解释每个变量的实际含义；
4. 最后说明该定义如何进入算法或实验。

不要直接以“定义4.1”开头。定义前至少增加一句动机，例如：

> 为区分真实零值与模态不可用状态，需要将观测状态和特征数值分开记录。下面定义每个位置、每个模态的观测状态。

### 8.2 结果段

结果段统一使用三层结构：

1. **事实：**表或图中的具体读数；
2. **解释：**该读数反映的模型现象；
3. **边界：**该结论适用的数据、模型和场景。

推荐句式：

> 从表/图中可以看到……（事实）。这说明……（解释）。该结论仅适用于……（边界）。

每个重要结论至少给出一个比较对象、一个指标和一个数据集划分。不要只写“效果明显”“贡献较大”“表现良好”。

### 8.3 事实、解释和结论分句

不要在一句话中同时塞入事实、机制解释和理论结论。建议分别写：

- 事实：表中数值是多少；
- 解释：为什么可能出现这个现象；
- 结论：本文最终能支持到什么程度。

例如，先写“文本缺失率升高时综合效用下降”，再写“这与文本信息在当前任务中的判别作用较强相符”，最后写“该结论不推广到其他数据集”。

### 8.4 主题句和段落闭环

每个段落第一句应明确本段的中心论点，后续句子只完成解释、证据或限制，不在段落中途突然更换主题。推荐段落结构为：

> 主题句 → 方法/理由 → 数据或公式证据 → 小结或过渡

段落不宜只有一两句，也不宜把三个以上独立论点塞在同一段。超过约60个汉字的长句应逐句检查；若同时出现多个从句、括号或破折号，优先拆分。60字是审查阈值，不是排版硬限制。

### 8.5 过渡词和内容引入

过渡词必须表示真实逻辑关系，不能用来掩盖推理跳跃：

- 顺承/递进：首先、其次、进一步、此外；
- 因果：因此、据此、从而；
- 转折/让步：然而、但、尽管如此；
- 条件：若、当……时、在……条件下；
- 对比：相比之下、与……相比、区别在于；
- 总结：综上、总体而言。

任何新观点、新概念、新指标、新方法或新数据出现前，都必须先说明“是什么、为什么出现、与前文有什么关系”。找不到铺垫、定义或依据的内容，视为“突然出现”，应补充过渡、前移定义或删除。

禁止以下三类“空降”：没有指标支撑的结论、没有定义的概念或指标、没有选择理由的算法和参数。

## 9. 图表解读标准

每张核心图表前后都要有文字连接。

图表必须遵循“引出—展示—解读”三步闭环。图表前说明该图表回答什么问题、横纵轴代表什么；图表后说明读者应从图中读出什么，以及结论的适用范围。

图表后至少写两句：

1. 读数演示：评委如何从图中读出结论；
2. 结论边界：该结论是否只适用于某个模型、数据集或场景。

推荐结构：

> 图5.x比较了……。从图中可以看到，……由……变化到……，而……基本保持不变。因此，……。该观察只说明……，不能直接推出……。

图题、正文和表格中的术语必须一致，不要在同一对象上交替使用“缺失率”“覆盖率”“抹除率”等未定义名称。

### 9.1 本论文图表的三步法具体写法

本论文图表的“引出—展示—解读”三步法对应以下具体句式。引出句必须包含该图回答的问题；展示句须包含图中横纵轴含义；解读句须给出“读数演示 + 结论边界”两个分句。

#### 引出句

推荐结构：

> 图 X.Y 比较了 [对象 A] 与 [对象 B] 在 [指标/场景] 上的差异。

示例（§5.4 退化曲线）：

> 图 5.Y(a) 比较了 MRFN 与 B3 在三模态单模态缺失率 10%—80% 下的综合效用退化曲线。

#### 展示句

推荐结构：

> 图中 [纵轴] 表示 [含义]，[横轴] 表示 [含义]，[曲线/列/颜色] 对应 [对象]。

示例：

> 图中纵轴为综合效用下降率 $D_\mathcal{S}$，横轴为缺失率 $r$（10%—80%），三条曲线分别对应文本、语音、视觉三模态的单模态缺失场景。

#### 解读句

必须包含两个分句：

- 读数演示：给出具体数字（差值、变化、区间），不写“明显/显著”；
- 结论边界：明确该结论适用于哪个数据集、模型或场景。

示例：

> 从图中可以看出，文本轨在缺失率 40% 时 $D_\mathcal{S}$ 约 0.067（MRFN）至 0.089（B3），而语音、视觉轨在各缺失率档位均保持在 0.005 量级。该结构说明退化主要来自文本模态，仅在 MRFN / B3 与本论文 31 场景网格内成立，不外推到缺失形态或缺失位置已扩展的实验。

## 10. 指标和数字口径

每个数字首次出现时必须能回答四个问题：

- 什么数据集划分；
- 什么模型或变体；
- 什么指标；
- 是单模型、种子均值还是集成结果。

正文中统一处理：

- Acc、macro-F1统一使用百分数或统一使用0—1比例，不可混用；
- MAE、Pearson和综合效用分保留统一小数位；
- 测试集终评与验证集选型明确分开；
- 单模型均值和多种子集成必须分别标注，不得直接比较；
- 每个结论句中的数字必须能回指表格、图或结果文件。

## 11. 三问之间的接口写法

三问不是三个孤立实验，应明确写出输入输出关系：

- 问题一输出统一位置、观测状态和位置—时间映射；
- 问题二使用这些定义训练缺失感知预测器；
- 问题三复用问题二的预测器，并沿问题一的映射回溯证据。

但不要把这种关系包装成“坐标资产贯通”“三层同构”等口号。直接说明“谁输出什么，谁使用什么”。

问题三中应明确区分：

- 主导模态：按 Shapley 贡献判断；
- 局部证据：按 IG 排序判断；
- 全局文本证据：指跨模态合并后的 IG TOP-k 统计。

避免把“文本主导”“视觉主导样本”和“文本TOP证据”写成相互矛盾的结论。

### 11.1 三问主线的章末过渡句模板

每章末段必须明确本章输出如何被下一章使用，禁止只写“接下来讨论……”式元叙述。

问题一章末过渡：

> 问题一输出的统一时序坐标、观测状态三元组与位置—物理时间映射，作为问题二训练和问题三解释的共同输入定义。

问题二章末过渡：

> 问题二输出的预测器 MRFN（含补充变体 MRFN+）与可解释接口（门控权重 $g_m$），作为问题三归因框架的输入；缺失构造规则保持与本章合成算子一致，使干预输入的状态与训练所见状态可对应。

问题三章末过渡：

> 问题三的解释输出与证据回溯链，可对照原始素材独立核验；其结构性边界（弱带文本分辨力、文本通道 LOO 反事实语义差异）由第七章统一汇总局限。

## 12. 推荐修改顺序

正文修改按以下顺序进行：

1. 重写第二章问题分析，建立三问路线图；
2. 统一第四至第六章开头的输入—处理—输出；
3. 清理自造术语和不必要引号；
4. 重写方法段，在公式前补动机句；
5. 重写结果段，补充读数演示和结论边界；
6. 统一图、表、正文的术语和数字口径；
7. 重写摘要，使其只保留主线、方法、核心结果和创新点；
8. 最后补公式编号、参考文献和全量结果表格。

## 13. 提交前正文检查清单

### 逻辑

- [ ] 每一问开头都说明任务、难点、方法和输出；
- [ ] 每个核心小节都能指出输入和输出；
- [ ] 每个主要结论都有对应图表或数字证据；
- [ ] 每个结论都写明适用范围；
- [ ] 三问之间的接口没有只用口号描述。
- [ ] 摘要和结论分别回应正文主线，没有引入新论证；
- [ ] 每章开头都有路线说明，每节都有明确主张；
- [ ] 新观点、概念、指标、方法和数据均有前置定义或引入理由；
- [ ] 相邻段落之间的逻辑关系能够用过渡句明确表达。

### 13.1 三问章节的专项检查

在通用清单之外，问题一、问题二、问题三的章节需各自检查以下要点：

#### 问题一章节专项

- [ ] 50 位网格的定义在第一次出现时给出（CLS / 内容 / SEP / 填充的结构状态）；
- [ ] “特征零值不自动等于缺失”这一前提在 4.1 节给出实测依据、在 4.2 节进入形式化定义；
- [ ] 边界延拓规则（R1—R5）与合法性判定（C1—C3）分别承担边界与失败两类情形，不能合并描述；
- [ ] 四类验收指标的定义、判定门槛、实测数字在 4.5 节同时给出，不分别散落；
- [ ] 回放图的选择规则（覆盖全部六类边界、至少含两条 rollback）在 §4.5 节明文登记；
- [ ] 与附件 2 的接口同源性核验（逐位余弦 ≥ 0.99）写明“11 条标定 + 7 条冻结后独立验证”协议，不写“调参后通过”。

#### 问题二章节专项

- [ ] 缺失空间四元组（M, P, r, l̄）的每个分量在 5.1 节首次出现时给出含义；
- [ ] 合成缺失算子 γ 的两条全局约束（结构位不抹除、零值不改变 o）写为独立的不可省略项；
- [ ] MRFN 三层机制（缺失状态编码、可用性约束注意力、可靠性门控）与第二章数据流图 G1 的环节一一对应；
- [ ] 31 场景网格的汇总规则（先每场景 8 实例均值、再 31 场景等权平均）单独成段，不与结论段混写；
- [ ] 选型分 𝒮_sel 的四个权重（0.50/0.25/0.15/0.10）写明各自倾斜的理由；
- [ ] 阶梯与消融分别承担“逐级增”与“移除则降”两端证据，不互相冒充；
- [ ] 测试集主表冻结、MRFN+ 追加不替换主表、不做 31 场景、不构造鲁棒性曲线，这一主次关系在 5.5 节首段明确写出；
- [ ] 附件 3 的全部产出为“预测结果与过程证据”，不作精度声明（锚点 5/5 仅作内部自检）。

#### 问题三章节专项

- [ ] “主导模态”（Shapley）、“局部证据”（IG）、“全局 TOP-k 文本证据”（IG 跨模态 TOP-3 统计）三者在 6.1 节定义处显式区分；
- [ ] 两套反事实参照分名（联盟级 v(∅)、条件特征基线）首次出现处写明各自用于 Shapley / IG；
- [ ] 解释框架不引入新可训练参数、不重新训练预测器，写在 6.1 节首段；
- [ ] 六项保真度检验（A—F）的判据与门槛在 6.4 节首段同时给出，结果按相同顺序报告；
- [ ] F 项未通过不补做实验、不移动门槛、不冒充完整性，结构性诊断写为独立的三步；
- [ ] 附件 4 全量预测与解释结果不做精度声明、回放图签核前一律称“自动回放”；
- [ ] 证据回溯链（位置 j → 原词 → 物理时间）写为单行的方向链，不写成“闭环”“贯通”等口号。

#### 第七章模型评价专项

- [ ] 7.1 优点四条不重复正文已证数字，引用回指而非重证；
- [ ] 7.2 三问联合验证只做“同向性”关系分析，不互相证明；
- [ ] 7.3 灵敏度与稳定性只汇总已有实测，不新增实验；
- [ ] 7.4 局限按四层（表示/预测/解释/实验覆盖）连续成段，错误归因只给结论+关键数字；
- [ ] 推广段落写明“结构对应 + 须另行验证”两件事，不写“可直接推广”“具有普适性”。

### 术语和行文

- [ ] 非标准术语均有必要性，并且至少重复使用三次；
- [ ] 普通词能够准确表达时，没有使用华丽或复杂替代表达；
- [ ] 首次出现的术语有一句白话解释；
- [ ] 不使用“坐标资产”“同构贯通”等包装性词语；
- [ ] 引号只用于定义、字段名或直接引用；
- [ ] 单句没有连续嵌套破折号、括号和数字口径；
- [ ] 一段只承担一个主要论点。
- [ ] 段首有主题句，段末有小结或过渡；
- [ ] 超过约60字的长句已检查主语、从句和插入语；
- [ ] 没有“模型知道/理解/主动选择”等未经定义的拟人化表达。

### 结果

- [ ] 每个指标都标注数据集划分和评估口径；
- [ ] Acc、macro-F1的单位和显示格式全文一致；
- [ ] 验证集选择结果和测试集终评结果分开；
- [ ] 图表后有读数演示，而不是只写结论；
- [ ] “显著”“明显”“主导”等判断均有数值或对照支持。
- [ ] 每张核心图表均完成“引出—展示—解读”闭环；
- [ ] 每个数字均能回指表格、图、公式、日志或结果文件；
- [ ] 未出现未定义指标、无依据参数或突然更换算法。

### 冗余

- [ ] 摘要、正文和模型评价没有原样重复同一组数字和结论；
- [ ] 正文没有逐行复述表格或逐句复述图注；
- [ ] 元叙述已压缩，保留的是路线、证据和结论；
- [ ] 包装性术语和同义口号只保留必要的一种；
- [ ] 过程日志、完整参数和逐实例结果已按需要移至附录或交付文件；
- [ ] 每段都能归入“保留、压缩、移附录、删除”之一。

## 14. AI辅助写作与原创性

### 14.1 目标边界

正文修改的目标是准确表达真实建模和实验工作，不是通过操纵句长、困惑度或词汇分布规避检测。任何检测器都可能误报，检测分数不能替代作者对内容真实性、数据来源和论证责任的确认。

### 14.2 允许采用的改进

以下改进同时有助于论文质量和作者性：

- 用真实、可回溯的实验细节替换空泛评价；
- 写清模型取舍、失败尝试和最终选择理由，但只能使用实验记录中确实发生过的内容；
- 用具体数字、样本、指标和对照替代“效果很好”“提升明显”等概括；
- 保留必要的专业术语和统一符号，不为改变词频而强行同义替换；
- 由作者逐段核对事实、公式、图表和引用，确认每个结论都能复现或回溯。

### 14.3 不采用的降率做法

不得为降低检测分数而：

- 随机打乱句长或故意制造语病；
- 大量同义词替换，破坏术语一致性；
- 使用机器改写、改写器或“降AIGC”工具生成未经人工核验的正文；
- 虚构调参经历、失败结果、样本细节或作者判断；
- 删除必要的逻辑过渡，使文章出现跳步；
- 把检测器可能偏好的语言特征当成固定写作规则。

### 14.4 本项目的人工核对重点

当前论文的概述性段落最容易模板化，重点检查摘要、第二章问题分析、各章开头和第七章模型评价。检查时不追求句式“像人”或刻意制造不规则，而是逐段确认：

1. 是否包含本项目真实的模型、数据和取舍；
2. 是否有对应的图表、公式或结果文件；
3. 是否明确了结论的适用范围；
4. 是否存在无法由实验记录解释的泛化评价；
5. 是否保留了必要的逻辑过渡和术语一致性。

还应做一次“模板化风险”检查，但只把它当作语言质量检查：

- 检查“首先/其次/最后”“综上所述”“值得注意的是”等连接词是否机械重复；必要的逻辑路标保留，重复的枚举式套话删除或改为具体承接；
- 检查摘要、章节开头和模型评价中的概述性句子是否有真实数据、模型取舍或实验依据；
- 检查段落长度和句式是否过度对称，但不要为了制造不规则而故意改坏逻辑或语法；
- 发现问题后按完整段落或小节重写，再回读上下文，不对检测报告中的单句做孤立同义替换。

论文作者应对最终文字负责。任何外部检测报告只能作为需要人工复核的提示，不能作为修改正文的唯一依据。

### 14.5 本论文的人工核对重点

下列位置在历次自动检查中已通过，但人工通读时仍需逐段复核：

- **摘要段**（约 250 字）：三个挑战的描述是否与正文 §1.1 完全一致、关键数字（66.71 / 68.64 / 100/100 / 1.0% / 19/20 / 5/5）是否与 §4.5、§5.5、§6.2、§6.4 的实测值一致；
- **第二章耦合关系段**：是否在统一坐标与特征来源之间显式区分（前者共用，后者不共用）；
- **第二章实验纪律段**：是否给出“验收门槛运行前冻结、未通过项如实报告”两项要点，不写“严格执行”“严格遵守”等口号；
- **4.5 章末假设验证段**：是否同时给出 H1、H2、H3 的判定结果（H2 成立、H1/H3 待人工签核），不写“全部成立”；
- **5.5 主次关系段**：是否明确“MRFN 为主表、MRFN+ 为补充、不替换、不做 31 场景”四项要点；
- **6.4 F 项诊断段**：是否给出三步结构性诊断，不写“不可解决”“未能通过”；
- **7.4 错误归因段**：是否同时给出 Z0/Z1/Z2 三带准确率与决策侧四种手段上限，不写“误差源于弱带”一句话；
- **7.4 推广段**：是否写明“结构对应 + 须另行验证”两件事，不写“可直接迁移”“具有普适性”。

### 14.6 本论文的模板化风险位置

下列位置在历次优化中已收敛，但人工通读时仍需检查是否再次出现机械重复：

- “首先……其次……最后……”三段式仅在 §2 章首段保留一次，其余位置改为具体承接；
- “综上所述”“由此可见”禁止作为小节末句；如需总结，仅写一句并指明结论边界；
- “值得注意的是”“需要指出的是”禁止作为段首短语；如需提示，直接进入事实句；
- “由此可见”“可见”禁止作为结论句开头；如需提示观察，写“实测显示”或“观察表明”；
- 摘要、章首段、模型评价三处是模板化风险最高的位置，通读时应优先复核这三段。

## 15. 冗余识别与删改规范

### 15.1 什么内容必须保留

以下内容即使篇幅较长，也不属于冗余：

- 题目要求但正文尚未明确回答的内容；
- 新变量、新指标、新算法或新规则的首次定义；
- 支撑核心结论的关键数字、比较对象和数据集口径；
- 公式推导中不能由上下文直接推出的关键步骤；
- 解释边界、失败结果和限制条件；
- 使读者能够复现实验或核对结果的必要参数；
- 从本章输出到下一章输入的接口说明。

判断标准不是“这句话是否重要”，而是“删去后读者是否无法理解、验证或复现本文结论”。

### 15.2 五类优先删除的内容

#### A. 同一事实的重复陈述

摘要、章节正文、图表说明和模型评价可以分别承担不同功能，但同一数字和同一结论不能原样重复多次。

处理规则：

- 摘要保留最终结论和最关键数字；
- 正文保留产生结论的比较和证据；
- 模型评价只做回指和综合，不重新复述整段实验；
- 同一结论在同一小节出现两次时，保留证据更完整的一次。

#### B. 表格或图表的逐字复述

表格已经列出完整数值时，正文不逐行重写。正文只回答：最大差异在哪里、趋势是什么、该趋势支持什么判断。

不推荐：

> B0为0.637，B1为0.657，B2为0.658，B3为0.650，MRFN为0.667……

推荐：

> MRFN的Acc最高，为66.7%；相较B0提高3.0个百分点。B1至B3的提升主要体现在回归指标，而非分类指标。

#### C. 没有新信息的元叙述

以下句子通常可以删除或压缩：

- “本节的主要内容如下”；
- “下面将对其进行详细介绍”；
- “由此可以看出，本节完成了……”；
- “为了更好地说明这一点，本文进一步……”；
- “需要指出的是，本文将……”（后文没有新的限定）。

如果元叙述确实提供章节路线，将其合并进开头的一句话；如果只是提示读者“接下来要读什么”，直接删除。

#### D. 包装性评价和同义口号

同一意义的多个表达只保留一个，并优先保留可核验的普通表达。例如以下内容不应同时出现：

- “坐标资产”“公共接口”“三问共同底座”；
- “缺失成为显式输入”“模型显式知道缺失”；
- “文本主导”“文本是主要脆弱性来源”“文本贡献占主体”；
- “闭环”“贯通”“同构”“统一框架”。

若这些词没有新增定义或数字证据，应删除；若确实需要总结，则保留一次并改为具体的输入输出关系。

#### E. 过程管理信息的重复出现

随机种子、冻结口径、预注册、测试集封版、代码指纹等信息对复现有价值，但不应在摘要、问题分析、实验结果和模型评价中反复出现。

处理规则：

- 首次出现处说明完整规则；
- 后文只写“按固定评测口径”或引用相应小节；
- 只影响交付而不影响论证的文件清单、历史轮次和日志细节移至附录。

### 15.3 可以压缩但不应删除的内容

有些内容不是冗余，但不必在正文展开：

- 工具的全部版本参数：正文保留工具、核心参数和作用，完整版本清单放附录；
- 失败样本的全部过程日志：正文保留失败类别、处置规则和比例，逐样本记录放附录；
- 八轮改进实验的逐轮过程：正文保留筛选原则、代表性负结果和最终采纳理由，完整日志放附录；
- 31个场景的全部逐实例数值：正文保留场景设计和汇总结果，逐实例数据随交付文件提供；
- 解释卡中重复的字段说明：正文解释展示逻辑，字段定义集中在一次说明。

### 15.4 删除、压缩和移动的判定流程

逐段处理时按以下顺序判断：

1. 该句是否直接回答题目、定义对象、给出证据或说明边界？若都不是，优先删除；
2. 该信息是否已经在前文完整出现？若是，保留一次，后文改为回指；
3. 该句是否只是重复图表已有数值？若是，改为趋势和差异解读；
4. 该内容是否对复现有用但会打断主线？若是，压缩为一句并移至附录；
5. 删除后是否造成逻辑跳跃？若是，不删除事实，只补一条过渡或合并句；
6. 删除后是否改变结论强度或适用范围？若是，保留限定语。

最终将每段归入四类之一：

- **保留：**承担论证、定义、证据或边界功能；
- **压缩：**信息有用，但存在重复或细节过多；
- **移附录：**复现有用，但不适合打断主线；
- **删除：**没有新增信息，也不承担逻辑连接。

### 15.5 当前论文的重点清理位置

优先检查以下位置：

- 第二章中三问路线、实验纪律和“统一坐标”概念是否重复说明；
- 第四章各小节反复出现的“本节的数学任务是”是否可以改为一次性路线说明；
- 第五章中表格数值、三条实验结论和第七章综合评价是否重复逐字复述；
- 第五章模型改进部分的过程管理句子是否挤占方法和证据空间；
- 第六章中 Shapley、IG、门控贡献和“文本主导”是否使用多个近义总结；
- 摘要中的方法名和评价性句子是否在正文开头再次完整重复。

删改的目标不是让每一段都短，而是让每一句都承担明确功能。保留必要的证据和限制，删除只增加语气、不增加信息的文字。

### 15.6 本论文的常见改写示例

下列示例来自本论文既有版本，按“问题句 → 改写句”格式给出，可在全文通读时参照执行。所有改动均不改变事实、数字与公式，仅调整句式与术语。

#### 示例一：包装性短语改为具体描述

- 原文（拟修改）：缺失增强训练以无干净代价换取鲁棒性增益。
- 改写：与仅使用干净样本训练相比，缺失增强训练使 31 个缺失场景的综合效用提高 0.030、干净综合效用提高 0.020；在本实验设置下未观察到干净性能下降。

#### 示例二：拟人化改为变量描述

- 原文（拟修改）：让模型显式“知道”每个位置每个模态是否被观测到。
- 改写：将每个位置、每个模态的可用状态作为输入变量，使模型区分“特征值为零”与“该位置不可观测”两种情况。

#### 示例三：定性总结改为量化描述

- 原文（拟修改）：缺失率升高时模型性能显著下降。
- 改写：在 31 个评测场景中，文本模态缺失率从 10% 升至 80% 时，综合效用下降率由 0.020 升至 0.183；语音、视觉模态在各缺失率档位均保持在 0.005 量级。

#### 示例四：结论强度与证据匹配

- 原文（拟修改）：充分证明了模型在文本缺失场景下的鲁棒性。
- 改写：在 31 个固定场景的评测中，文本缺失 40% 时综合效用下降率约为 0.07—0.09、80% 时约为 0.18—0.26；该结果支持“文本缺失造成的退化大于语音与视觉”的判断，未做统计显著性检验。

#### 示例五：跨章接口改为具体描述

- 原文（拟修改）：三问采用统一的坐标资产贯通框架。
- 改写：问题一输出统一时序坐标、观测状态三元组与位置—物理时间映射；问题二使用该坐标系训练 MRFN/MRFN+，问题三复用同一预测器并沿该映射回溯证据。

#### 示例六：综述性数字改为差异化引用

- 原文（拟修改）：模型在测试集上达到了 68.64% 的准确率，显著优于基线。
- 改写：测试集封版 MRFN 单模型准确率 66.71%，预注册协议筛选采纳的 MRFN+ 三种子集成为 68.64%；单模型均值与集成属不同评估口径，不作直接比较。

#### 示例七：抽象方法名改为输入输出关系

- 原文（拟修改）：本章完成了缺失空间的构造。
- 改写：缺失空间被形式化为四元组 (M, P, r, l̄)，使缺失模态类型、缺失位置、缺失时长成为算子的显式参数；合成缺失算子 γ 在该空间上定义，输出可用状态 $a=o\cdot(1-b)$。

#### 示例八：方法动机前置

- 原文（拟修改）：定义 4.1 设位置集 J 为 50 个序列位置。
- 改写：附件 2 对齐版本将三模态组织在同一 50 位序列上，为使跨模态信息可逐位置联合建模，需要先固定位置体系。定义 4.1 给出的语义位置集 $J$ 即为此体系的形式化。
